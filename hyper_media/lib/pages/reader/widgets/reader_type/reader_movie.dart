import 'package:flutter/material.dart';

class ReaderMovie extends StatelessWidget {
  const ReaderMovie({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ReaderMovie"),
    );
  }
}
