import 'package:flutter/widgets.dart';

class ExtensionLoadError extends StatelessWidget {
  const ExtensionLoadError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}